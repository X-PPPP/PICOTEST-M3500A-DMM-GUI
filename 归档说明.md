# M3500A USB 逆向与上位机 —— 项目归档说明

> 本文档完整记录本项目从"设备无法驱动"到"做出上位机并打包归档"的全过程，便于日后查阅与复现。
>
> 关键词：PICOTEST M3500A · USB 厂商私有设备 · WinUSB/Zadig · pyusb · SCPI · 逆向 · DmmUpdate · 校准

---

## 0. 项目概览

| 项目 | 内容 |
|---|---|
| 目标 | 让一台 USB 无法驱动的 PICOTEST **M3500A** 六位半万用表在 Windows 11 上可用 |
| 结论 | **无需原厂驱动**：设备 USB 就是"裸 SCPI 透传"，绑通用 WinUSB 即可用 pyusb 直接控制 |
| 交付物 | SCPI 驱动 (`m3500a.py`)、Tkinter 上位机 (`m3500a_gui.py`)、探测脚本、打包 exe、本归档 |
| 未做 | 未刷固件（有风险且无必要）；未获取/使用原厂私有驱动 |

---

## 1. 背景与初始症状

- 仪器：PICOTEST M3500A（6½ 位数字万用表，实为 Array / Keithley 2100 同平台 OEM），序列号 `TW00001836`。
- 固件：DSP **1.0G**（面板 `MENU → SYSTEM VER.` 显示 `01.0G-01-04`）。
- 症状：插上 USB 后，Windows 设备管理器显示 **Code 28（未安装驱动程序）**，无法被任何软件使用。
- 面板测量功能一切正常，问题仅在 USB 远程接口。

---

## 2. 环境

| 项 | 值 |
|---|---|
| 主机 | Windows 11 x64 |
| Python | 3.14 |
| 通用 USB 驱动 | **Zadig**（随 sigrok/PulseView 安装），给设备装 **WinUSB** |
| Python 库 | `pyusb`、`libusb-package` |
| 打包 | PyInstaller 6.22 |
| 其它工具 | 7-Zip、GNU binutils（objdump/strings）、x86dbg（备用） |

---

## 3. 侦察：读设备真实 USB 描述符

通过 USB Hub IOCTL 读原始描述符（脚本见 `planB_usb_probe.py` 的思路，早期用 PowerShell + P/Invoke 实现）：

- **设备描述符**：`12 01 00 01 FF 00 00 10 4E 16 AC 0D 00 01 02 03 01 01`
  - `bcdUSB = 1.00`（USB 1.1，full-speed）
  - `bDeviceClass = FF / 00 / 00`（厂商自定义）
  - `idVendor = 164E`，`idProduct = 0DAC`
- **配置描述符**：1 个接口，class `FF / A0 / B0`（厂商自定义），**没有 USBTMC 类描述符**
- **端点**：
  | 端点 | 方向 | 类型 | 包长 |
  |---|---|---|---|
  | `0x81` | IN | 中断 | 16 |
  | `0x01` | OUT | 中断 | 16 |
  | `0x82` | IN | 批量 | 64 |
  | `0x02` | OUT | 批量 | 64 |
- USB 主控：**PDIUSBD12**（服务手册 BOM：U2101）

**判断**：接口是厂商自定义类（非 USBTMC `FE/03/01`），Windows 没有内置驱动可匹配 → Code 28。

---

## 4. 原厂软件是怎么访问它的

应用程序/升级工具的 I/O 层是 `ins003.dll`（安装后位于 `C:\Windows\System\ins003.dll`）：

- 通过 **设备接口 GUID `{7036090D-B21C-4B65-9AEA-7CA44D867F67}`** 用 SetupAPI 枚举设备；
- `CreateFileA` 打开后，用两个**自定义 IOCTL**通信：
  - `0x22800A`：写（输入 N 字节，输出 64）
  - `0x224209`：读（输入 64，输出 64）
- 该 DLL 还支持 GPIB（`GPIB-32.DLL` / `agvisa32.dll` / `visa32.dll`）与 USBTMC
  （资源串 `USB[0-9]*::0x164E::0x0DAD::?*INSTR`，注意 **PID 0x0DAD**）。

> 这两个 IOCTL 属于一个**原厂自定义内核驱动**；本机没有，任何下载包里也没有
> （已全盘搜索 `.inf/.sys/.cat` 确认）。名为 `M3500A-USB-2000xp-driver.zip` 的包
> 解包后其实**只有应用程序**（PT_TOOL/MyLink/ins003/OCX），没有任何驱动文件。

---

## 5. 寻找驱动的曲折

1. 解包全部 Picotest/Array 下载包 → **均无驱动**（只有 app）。
2. 对比 **Keithley 2100**（同平台）的 IVI 驱动包 → 其 `Driver.cab` 是 **VISA 之上的 IVI 用户态 DLL**，且 2100 走 USBTMC，与 1.x 私有接口不兼容。
3. 用 NI-VISA Driver Development Wizard 生成 INF → 只能绑 **WinUSB（裸 USB）**，不是原厂那套 IOCTL。
4. 联系 PICOTEST 原厂，回复要点：
   - 机器约 20 年、硬件多次改版，**不建议为改善 USB 而刷固件**（可能计量值偏差、校准过不了）；
   - 固件升级**仅能通过 USB**，不能用 GPIB/RS-232；
   - 若只需连电脑读数记录，建议改走 **GPIB 或 RS-232**；
   - 返厂可尝试但**不保证成功、不做校准验证**，运费/检测费自理。

**结论：原厂驱动这条路基本关闭。**

---

## 6. 突破：设备其实是"裸 SCPI 透传"

给设备绑 **WinUSB**（Zadig）后，用 pyusb 直接发命令试探：

```
OUT 0x02 (bulk)  ->  b"*IDN?\n"
IN  0x82 (bulk)  ->  b"PICOTEST,M3500A,1,01.0G-01-04"
（OUT 0x01 / IN 0x81 中断端点同样可用）
```

**设备本身就是一条 SCPI 字节管道**，原厂那个"自定义驱动"只是个薄转发层。

### 分帧注意事项（实测）
- 命令以 `\n` 结尾即可；
- **不要补零**（补到 16/64 字节会超时）；
- 读取时轮询到短包/超时即认为结束。

→ 从此**不需要原厂驱动**，用 `m3500a.py` / GUI 即可完整控制。

---

## 7. 固件升级调查（**不推荐执行**）

### 7.1 两个升级工具的区别
| 工具 | 版本 | 通信方式 | 用途 |
|---|---|---|---|
| `M3500_FW_UPDate_1.0H\DmmUpdate.exe` | v1.035 | 原厂私有驱动（GUID + IOCTL） | 1.x 用 |
| `M3500_FW_UPDate_3.02/3.03\DmmUpdate.exe` | v2.06 | NI-VISA + USBTMC（`USB...::0x164E::0x0DAD::?*INSTR`） | 3.x 用 |

2.06 只能对**已经是 USBTMC（3.x）**的机器升级，无法用于 1.x。

### 7.2 v1.035 刷写协议（静态逆向结果）
从 `DmmUpdate.exe` 提取的常量与流程：
- 字符串 `*idn?`、`system:update`；
- 流程：`*idn?` 检测设备 → 写 `system:update`（10 字节）进入升级模式 →
  按 **0x2000 字节/块** 读文件并逐块写出 → 读回响应判断（比对字符 `'0'`）；
- `5407_*.pak` 是"包含多个 bin 的归档"（文件头 `This archive contains several bin files.`）。

理论上可用 pyusb 复现该流程。

### 7.3 风险评估（为何不做）
- 服务手册明确：**固件与校准数据存在同一片 flash**（MX29LV400，Boot Top）；
  1.x → 3.x 跨大版本刷新很可能影响/清除校准数据 → 精度变差。
- 原厂已声明不保证成功、不做校准验证。
- 既然"能连电脑"已由第 6 节解决，**刷机收益小、风险大**，故不执行。

---

## 8. 校准调查

- 服务手册 §6 "Simple Calibration" 的面板路径：
  `MENU → PREV → CAL MENU → ENTER → SECURED → ENTER → CODE → ENTER → 输入 123456 → ENTER`，
  之后 `UNSECURED → NEXT → CALIBRATE → ENTER`；默认安全码 `123456`（可用 PT-SET 改）。
- **实测 1.0G 固件**：
  - `CAL:PROT:STAT?` → `0`（存在校准保护子系统）
  - `CAL:PROT:CODE` / `CAL:SEC:CODE` 命令存在（查询形式报 `Invalid separator`）
  - 但 `CAL:VOLT:DC?` / `CAL:CURR:DC?` / `CAL:RES?` / `CAL:COUN?` / `CAL:DATE?` 等**全部 `-113 Undefined header`**
- 结论：**1.0G 没有逐档校准命令**，前面板 `CAL MENU` 也只在 3.x 上有。
  即 1.0G 基本无法自行校准；要么接受现状，要么冒风险刷 3.x 或送厂。

---

## 9. 面板文字显示（`CrackedBy DS4.1f`）

这是**标准 SCPI 命令**（仪器自带接口），不是漏洞：

```
DISP:TEXT "CrackedBy DS4.1f"   ; 前面板下排显示（最多 16 字符）
DISP:TEXT?                      ; 读回
DISP:TEXT:CLE                   ; 清除
DISP ON|OFF                     ; 关/开显示
```

---

## 10. 上位机（GUI）开发

文件：`m3500a_gui.py`（Tkinter，单文件，无第三方 GUI 依赖）。

- **测量页（Measure）**：功能、量程、**测量模式 Mode**、自动调零、
  **数字滤波**（`Avg filter` 勾选 + `MOVing/REPeat` + 点数）、单次/连续读取、间隔；
  大字号读数、统计(n/min/max/avg/σ)、实时曲线（Tk Canvas 自绘）、CSV 记录/导出。
- **测量模式（Table 4-1）**：`Fast/Slow 4.5/5.5/6.5` 六档，分别设置
  NPLC 与 AutoZero/AutoGain（见第 12.1 节）。切档会**自动给出推荐间隔**（可手动覆盖），
  参数表以图例形式放在主界面**左下角**。
- **数字滤波**：`AVER:STAT`（勾选开关）+ `AVER:TCON`（MOVing/REPeat）+ `AVER:COUN`（点数）。
  另保留 AC 模拟滤波 `DET:BAND`（在 SENSe 页）。
- **数值显示格式**：`Auto`（友好）/ `Raw`（仪表原始 9 位有效数字）/ `Sci`。
- **NULL 快捷键**：取当前读数为相对零点并开启 NULL 数学
  （`CALC:NULL:OFFS <读数>` → `CALC:FUNC NULL` → `CALC:STAT ON`），再按一次关闭；
  测电阻（尤其 2 线）时消除表笔/引线电阻。实测开启后读数归零、关闭恢复。
- **SENSe / Temperature / Math / Trigger / System / Status / Console** 覆盖手册附录 C 命令；
  NPLC、闸门时间、带宽等细调放在 **SENSe** 页。
- **中英文切换**（`EN / 中文`），实时生效。
- **不支持项标注**：1.0G 缺失的 `TCOU:*` 标注 `(n/a)` 并禁用按钮。
- **取值映射提示**：每行灰色小字解释缩写 + "Get" 读回值对应的文字：
  自动量程 `1 (ON)`、单位 `C (Celsius)`、触发源 `IMM (Immediate)` 等。
- **高速采集**：`SENSe → Speed` 页有 `Fast preset` 按钮与提示。

### 10.1 测量模式与间隔（Table 4-1）

| Mode | NPLC | AutoZero(AutoGain) | 推荐间隔(s) |
|---|---|---|---|
| Fast 4.5 | 0.02 | off / off | 0.02 |
| Slow 4.5 | 0.1 | on / on | 0.05 |
| Fast 5.5 | 0.1 | off / off | 0.05 |
| Slow 5.5 | 1 | on / on | 0.1 |
| Fast 6.5 | 1 | on / on | 0.1 |
| Slow 6.5 | 10 | on / on | 0.5 |

> 实测细节：本机固件把"分辨率(RES)"与 NPLC **耦合**——设 NPLC 会联动 `RES?`
> （0.02→4.5 位、0.1→5.5 位、1→6.5 位、10→更高），反向设 `RES` 也会把 NPLC 拉回对应档。
> 因此远端无法做出"4.5 位 + NPLC 0.1"这类组合；Slow 档显示位数会随 NPLC 上涨。
> `RES` 的 `MIN`=最细(慢) / `MAX`=最粗(快) / `DEF`=默认。主界面已移除冗余的 Resolution，
> 仅保留在 SENSe 页。

### 10.2 采集流畅性（实测）

- 绘图 ~0.45ms/次，**不是**瓶颈；USB 也很快（NPLC0.02 时 `READ?` 仅 ~2.3ms）。
- 卡顿来自**仪表在 NPLC=1 时的偶发 ~365ms 抖动**（每几十次一次，测量侧）。
- 结论：小间隔采集请用 **NPLC ≤ 0.1 或 Fast preset**（并关自动调零），即顺滑。
- GUI 侧已做：按周期调度采样、批量处理读数、绘图节流(~8fps)、CSV 批量 flush。

---

## 11. 打包 exe

```bash
pip install pyinstaller
python -m PyInstaller --noconfirm --clean --onefile --windowed --name M3500A_GUI \
  --collect-all libusb_package --hidden-import usb.backend.libusb1 m3500a_gui.py
```

产物 `dist/M3500A_GUI.exe`（约 16 MB，含 libusb-1.0.dll）。
自检（GUI 无法脚本点击，故内置 CLI 自检）：

```
M3500A_GUI.exe --selftest
# 写入 m3500a_selftest.txt:
#   backend=libusb_package
#   IDN=PICOTEST,M3500A,1,01.0G-01-04
#   READ=+6.62637504E-03
#   RESULT=OK
```

---

## 12. 命令支持矩阵（固件 1.0G 实测）

- ✅ 支持：`MEASure:*?`、`CONFigure:*`、`CONF?`、`FUNC?`
  `SENS:<func>:NPLC/RANG/RANG:AUTO/RES`、`SENS:FREQ/PER:APER`、`SENS:DET:BAND`、
  `SENS:ZERO:AUTO`、`SENS:GAIN:AUTO`、`INP:IMP:AUTO`、`UNIT`、`TEMP:TRAN`、
  `TEMP:RTD:TYPE/RZER/ALPH/BETA/DELT`、`TEMP:SPRTD:RZER/A4/B4/AX/BX/CX/DX`、
  `CALC:*`（FUNC/STAT/PERC/NULL/LIM/MXB/DB/DBM/AVER）、`TRIG:*`、`SAMP:COUN`、
  `DATA:POIN/FEED`、`DISP*`、`SYST:BEEP*`、`SYST:VERS`、`SYST:ERR`、
  `*ESE/*SRE/*PSC/*ESR/*STB/*OPC`、`STAT:QUES:*`、`ROUT:TERM?`
- ❌ 不支持：**热电偶 `TCOU:*`** —— `TCOU:TYPE`、`TCOU:RJUN:RSEL`、`TCOU:RJUN:SIM`、`TCOU:RJUN:REAL:OFFS`（`-113 Undefined header`）

---

## 13. 复现步骤（从零）

1. 用 **Zadig** 给 `VID_164E/PID_0DAC` 安装 **WinUSB**。
2. `pip install pyusb libusb-package`
3. `python m3500a_gui.py` → Connect（或 `python m3500a.py`）。
4. （可选）打包 exe（见第 11 节）。

---

## 14. 关键常量速查

```
VID:PID（1.x 私有 USB）   164E:0DAC
3.x USBTMC PID            164E:0DAD
接口类                    FF/A0/B0
端点                      EP1 中断 16B (0x01/0x81), EP2 批量 64B (0x02/0x82)
原厂接口 GUID             {7036090D-B21C-4B65-9AEA-7CA44D867F67}
原厂 IOCTL                写 0x22800A, 读 0x224209
SCPI over USB             OUT 0x02 + "\n"，IN 0x82 读回
升级命令                  system:update (10B) → *idn? → 0x2000 分块发文件
面板文字                  DISP:TEXT "..."
校准(3.x)                 MENU→PREV→CAL MENU→…→123456
```

---

## 15. 时间线（过程回顾）

1. 设备 Code 28，无驱动。
2. 读 USB 描述符 → 确认厂商自定义接口、非 USBTMC。
3. 分析 `ins003.dll` → 得到原厂 GUID 与两个 IOCTL。
4. 全盘找驱动未果；解包下载包确认无驱动；对比 Keithley 2100（USBTMC，不通用）。
5. 邮件咨询 PICOTEST → 不建议刷机，建议 GPIB/RS-232；驱动请求被回避。
6. 尝试方案 B：Zadig 装 WinUSB + pyusb 探测 → **发现是裸 SCPI 透传，通信成功**。
7. 逆向 `DmmUpdate.exe` v1.035 → 提取 `system:update` + 分块刷写协议。
8. 探测校准命令 → 确认 1.0G 仅有 `CAL:PROT`，无逐档校准。
9. 开发 Tkinter 上位机（覆盖附录 C），加中英切换、n/a 标注、取值映射。
10. PyInstaller 打包 exe 并自检通过。
11. 归档整理（本仓库）。

---

## 16. 合规与风险

- 本仓库**不包含**任何厂商固件、手册或安装包（版权归 PICOTEST / Array / Keithley 所有）。
- **刷写固件有变砖风险**，且会触碰与校准数据共享的 flash；本项目**不提供刷写工具**。
- 一切操作风险自负。

---

## 17. 文件清单（本仓库）

| 文件 | 说明 |
|---|---|
| `README.md` | 项目简介与快速开始（中英） |
| `归档说明.md` | 本文件（完整过程记录） |
| `m3500a.py` | 低层 SCPI-over-USB 驱动 + CLI demo |
| `m3500a_gui.py` | Tkinter 上位机 |
| `planB_usb_probe.py` | 端点/分帧探测脚本 |
| `requirements.txt` | Python 依赖 |

---

## 18. 工具与来源

- [Zadig / libwdi](https://github.com/pbatard/libwdi)（通用 USB 驱动安装）
- [pyusb](https://github.com/pyusb/pyusb) + [libusb-package](https://github.com/pyusb/libusb-package)
- [sigrok PulseView](https://sigrok.org/)（Zadig 来源）
- GNU binutils（objdump/strings，用于静态分析）、x86dbg（备用）
